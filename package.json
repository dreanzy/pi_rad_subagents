{
  "name": "rad-subagents",
  "version": "0.1.0",
  "description": "Subagent Tool for Pi Agent — delegate tasks to specialized agents with isolated context windows.",
  "license": "MIT",
  "type": "module",
  "files": [
    "extensions"
  ],
  "exports": {
    "./extensions": "./extensions/index.ts"
  },
  "pi": {
    "extensions": [
      "./extensions"
    ]
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "ci": "npm run typecheck && npm test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dreanzy/pi_rad_subagents.git"
  },
  "bugs": {
    "url": "https://github.com/dreanzy/pi_rad_subagents/issues"
  },
  "homepage": "https://github.com/dreanzy/pi_rad_subagents#readme",
  "engines": {
    "node": ">=22.19.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-agent-core": "^0.79.10",
    "@earendil-works/pi-ai": "^0.79.10",
    "@earendil-works/pi-coding-agent": "^0.79.6",
    "@earendil-works/pi-tui": "^0.79.10",
    "@types/node": "^26.0.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  },
  "dependencies": {
    "typebox": "^1.3.1"
  }
}
